%h1 All Lists

%table.table.table-striped
  %thead
    %tr
      %th Title
      %th Description
      %th Item Count
  %tbody
    - @lists.each do |list|
      %tr
        %td= link_to list.title, list
        %td= list.description
        %td= list.items.count
